menuconfig FS_FAT
	bool
	select FS_LEGACY
	prompt "FAT filesystem support"

if FS_FAT

config FS_FAT_WRITE
	bool
	prompt "FAT write support"

config FS_FAT_LFN
	bool
	prompt "Support long filenames"

endif
